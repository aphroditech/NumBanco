# Copy to `.env` in this folder (Create React App reads REACT_APP_* at build time).
# Must match where your NumBanco API is reachable (same machine: localhost; phone/other PC: your LAN IP).

REACT_APP_API_URL=http://localhost:5000/api
