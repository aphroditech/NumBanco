# Copy to `.env` in this folder (Create React App reads REACT_APP_* at build time).
# Must match where your NumBanco API is reachable (same machine: 192.168.131.27; phone/other PC: your LAN IP).

REACT_APP_API_URL=http://192.168.131.27:5000/api
